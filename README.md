# AI Upgrade Path
A simple onboarding system for AI enthusiasts.

## Features
* Quick sign-up via email
* Role and goal selection
* Auto-generated roadmap for the first week
* Secure user data storage

## Usage
1. Run `python -m pytest` to test the system.
2. Use the `onboarding` module to create a new user and generate their roadmap.
